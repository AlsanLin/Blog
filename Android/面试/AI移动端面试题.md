
## 端侧模型部署

### 模型格式与推理引擎
- TFLite / ONNX / MNN / NCNN / 厂商自有引擎（华为 HiAI / 高通 SNPE）的对比？各自适用场景？
- TFLite 的 `.tflite` 文件结构和 `.pb`（TF GraphDef）的区别？模型转换时做了什么？
- ONNX 作为中间表示的优势？你实际用过的模型转换流程是怎样的（PyTorch → ONNX → TFLite）？
- 端侧推理引擎的算子支持——如果模型里某个算子端侧不支持怎么办？（算子回退到 CPU / 替换算子 / 后端扩展）

### 模型量化
- INT8 量化的三种方式（动态范围量化、全整数量化、float16 量化）的区别和精度影响？
- 量化校准（Calibration）是什么？为什么全整数量化需要代表性数据集？
- 模型量化后精度下降超过预期，怎么办？（逐层精度分析 / per-channel 量化 / QAT 量化感知训练）
- 你在实际项目中做过量化吗？精度损失了多少？有没有某个算子量化后效果特别差的例子？

### 推理加速
- GPU Delegate、NNAPI Delegate、CoreML Delegate 各自的底层硬件是什么？怎么选择？
- NNAPI 在不同厂商设备上的行为差异——有没有遇到过某个设备上 NNAPI 推理结果和 CPU 不一致？
- 使用 NNAPI 的 `nnapi-allow-fp16` 选项有什么好处和风险？
- XNNPack delegate 是什么？和 GPU Delegate 比有什么不同？
- 多线程推理（`setNumThreads`）的效果？为什么不是线程越多越快？

### 内存与性能
- 端侧模型加载后占用多少内存？怎么测量？
- 模型推理的功耗怎么评估？连续推理对手机发热的影响？
- 模型推理在哪个线程执行？主线程推理的后果？你是怎么调度推理任务的？
- 多个模型同时加载时，内存和 GPU 资源如何管理？

---

## 云端大模型接入

### API 集成
- 云端大模型 API（OpenAI / 讯飞星火 / 文心一言 / 通义千问）的接入方式？
- 大模型 API 的流式输出（SSE / WebSocket）在 Android 端怎么处理？事件流的解析和 UI 更新？
- 流式输出的中断处理——用户取消请求时，你怎么通知服务端停止生成？
- API 调用的重试和超时策略？大模型接口响应时间通常比较长，怎么设计用户体验？

### Prompt 工程
- 你在讯飞实际用过的 Prompt 策略？（System Prompt / few-shot / Chain-of-Thought）
- Prompt 是如何管理版本的——是在客户端写死还是服务端下发？为什么这么选择？
- 多轮对话的上下文管理——对话历史怎么裁剪？token 窗口满了怎么处理？
- 用户输入的敏感内容过滤——是在客户端、网关层还是模型层做？

### 混合架构（端 + 云）
- 哪些场景用端侧推理、哪些场景用云端模型？你的分流策略是什么？
- 网络不可用时，端侧模型如何兜底？端侧模型和云端模型的输出格式如何统一？
- 端侧小模型和云端大模型的结果不一致时，以哪个为准？有没有投票或仲裁机制？
- 端侧模型更新策略：是和 App 版本绑定、还是独立热更新？

---

## AI 功能落地 Android

### 典型场景
- AI 语音评测（跟读/背诵打分）：技术链路是怎样的？（录音 → VAD 检测 → ASR → 评测引擎 → 反馈 UI）
- AI 批改作业（OCR + NLP）：拍题 → 文字识别 → 答案匹配 —— 每个环节的技术选型和难点？
- AI 智能推荐：推荐模型是跑在端侧还是服务端？特征工程在 App 层做了什么？
- AI 对话助手：聊天 UI 的实现（消息列表、Markdown 渲染、代码高亮、流式打字效果）
- 实时语音对话：低延迟语音交互怎么实现的？（WebSocket + Opus 编码 + VAD 打断）

### 工程化
- AI 功能的 A/B 实验怎么设计？如何量化 AI 功能对用户留存/使用时长的提升？
- AI 功能的上线节奏——先灰度再全量？灰度期间的监控指标有哪些？
- AI 推理的耗电/发热问题怎么监控和优化？有没有用户因为发热卸载 App 的数据？
- 端侧模型文件比较大——怎么在 APK 中分发？动态下载 vs 内置的 trade-off？

---

## AI 辅助开发

### Cursor / Claude / Copilot
- 你现在日常开发中 AI 工具的使用比例？哪些环节用 AI、哪些完全手写？
- AI 生成的代码在 Code Review 时怎么审？有没有 AI 生成代码导致线上 Bug 的经历？
- AI 工具对团队开发流程的影响——有没有给团队制定过 AI 辅助开发的规范？
- Prompt 工程能力在开发中的实际应用——怎么写出好的开发 Prompt？举一个你实际用过的例子
- 你认为 AI 会让 Android 开发者的哪些技能贬值？哪些技能反而更重要？

---

## 前沿方向

### Gemini Nano / AICore
- Android 14 引入的 AICore 是什么？和传统的 TFLite 部署有什么区别？
- Gemini Nano 能在哪些设备上运行？硬件要求是什么（Google Tensor / 骁龙 8 Gen 3）？
- AICore 的 Safety 模块做了什么？和普通大模型的输出过滤有什么不同？

### MLKit
- Firebase ML Kit 的云端/端侧模式切换机制？
- ML Kit 的文字识别（OCR）和自训练模型比，精度如何？什么场景下自己训更好？
- ML Kit 的图像标签、人脸检测等 API 的底层分别用了什么模型？

### 三方框架
- MediaPipe 在 Android 端的集成方案？和 TFLite 的关系？
- HuggingFace 的 `transformers` 在 Android 上的使用现状？`sentence-transformers` 能做端侧语义搜索吗？
- llama.cpp 的 Android 移植——你有关注过吗？性能表现如何？

---

## 安全和隐私

- 端侧模型文件如何防逆向？模型可以被直接提取和盗用吗？
- 用户的个人信息（如语音、照片）用于推理时，如何保证不泄露？（边缘计算 vs 云端上传）
- AI 生成内容的合规性——如何防止大模型输出违规内容？Android 端有什么特殊的审核要求？
- 端侧个性化模型的隐私优势——Federated Learning 在 Android 上的实现现状？

